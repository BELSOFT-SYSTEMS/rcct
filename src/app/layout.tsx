import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "RTCC Concepts Ltd — Engineering Excellence in Nigeria",
  description: "Civil engineering, telecommunications, oil & gas, and procurement services across Nigeria since 2008. RC1150194.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
